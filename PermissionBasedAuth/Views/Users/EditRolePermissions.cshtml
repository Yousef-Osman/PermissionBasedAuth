@model RolePermissionsVM;

@{
    ViewData["Title"] = "Edit User Roles";
}

<section class="col-md-8 mx-auto">
    <div class="card shadow p-5 mt-5">
        <div class="d-flex mb-3">
            <h4 class="me-auto text-dimgrey">User Roles</h4>
        </div>
        <form method="post">
            <div class="mb-3">
                <label class="form-label" asp-for="@Model.RoleName"></label>
                <input class="form-control" asp-for="@Model.RoleName" readonly>
            </div>
            <label class="form-label" asp-for="@Model.Permissions"></label>
            <input asp-for="@Model.RoleId" hidden />
            <ul class="list-group">
                @for (int i = 0; i < Model.Permissions.Count(); i++)
                {
                    <li class="list-group-item">
                        <div class="d-flex justify-content-between align-content-center my-1">
                            <input asp-for="@Model.Permissions[i].Name" hidden>
                            <p class="mb-0 align-self-center">@Model.Permissions[i].Name</p>
                            <input class="form-check-input" asp-for="@Model.Permissions[i].IsSelected">
                        </div>
                    </li>
                }
            </ul>
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="d-flex justify-content-end">
                <button type="submit" class="btn btn-success me-1">Save</button>
                <button asp-action="Roles" class="btn btn-outline-secondary">Cancel</button>
            </div>
        </form>
    </div>
</section>